#ifndef USERDATATYPE 
#define USERDATATYPE

#include <stdlib.h>

#define Placeholder "%d" // "%d" OR " %c" (because we're reading '\n' also) OR "%f" 
typedef int UserDataType; //int OR double OR char OR float ...

#endif